# Budgets + feature flags per milestone (Docs/infra/INFRASTRUCTURE.md §3).
budgets:
  memory_tokens: 600        # PROMPTS.md §2 [3]
  conversation_tokens: 2000 # PROMPTS.md §2 [7]
  context_tokens: 4000      # PROMPTS.md §2 [6]

features:
  webui: true      # Alan_T web client served at /
  memory: true     # M1
  knowledge: true  # M2
  telegram: true   # M2
  supervisor: true  # M3 — ROUTER-model routing + skill selection
  voice: true      # M6 — STT in + TTS voice replies (live S2S deferred)
